{
  "name": "@hatch-radar/analysis",
  "version": "0.1.0",
  "private": true,
  "description": "AI 分析能力：analyzer 引擎(prompt / 洞察 schema / Anthropic·OpenAI 兼容客户端 / 处理器装配) + analysis-config(模型解析·热重载·入队) + analysis.service(洞察落库)。api 用配置片入队、worker 用引擎执行。",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.177",
    "@anthropic-ai/sdk": "^0.104.1",
    "@hatch-radar/db": "workspace:*",
    "@hatch-radar/kernel": "workspace:*",
    "@hatch-radar/shared": "workspace:*",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@hatch-radar/config": "workspace:*",
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "^4.1.8"
  }
}
