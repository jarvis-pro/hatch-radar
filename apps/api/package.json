{
  "name": "@hatch-radar/api",
  "version": "0.1.0",
  "private": true,
  "description": "工作台控制面 API（NestJS + PostgreSQL）：HTTP + 鉴权 + 调度 + push 网关 + 同源托管 web SPA。领域逻辑组装在 src/domain（复用 kernel/db/crawler/analysis 能力包）。密钥只在此进程，绝不下发到 web / mobile。",
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @hatch-radar/db db:migrate && node --env-file-if-exists=../../.env --env-file-if-exists=.env --watch --import @swc-node/register/esm-register src/main.ts",
    "start": "pnpm --filter @hatch-radar/db db:migrate && node --env-file-if-exists=../../.env --env-file-if-exists=.env --import @swc-node/register/esm-register src/main.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@hatch-radar/analysis": "workspace:*",
    "@hatch-radar/auth": "workspace:*",
    "@hatch-radar/config": "workspace:*",
    "@hatch-radar/crawler": "workspace:*",
    "@hatch-radar/db": "workspace:*",
    "@hatch-radar/kernel": "workspace:*",
    "@hatch-radar/shared": "workspace:*",
    "@nestjs/common": "^11.1.26",
    "@nestjs/config": "^4.0.4",
    "@nestjs/core": "^11.1.26",
    "@nestjs/platform-express": "^11.1.26",
    "@nestjs/schedule": "^6.1.3",
    "@nestjs/serve-static": "^5.0.3",
    "better-sqlite3": "catalog:",
    "nestjs-pino": "^4.6.1",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.2",
    "ws": "^8.21.0",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@nestjs/testing": "^11.1.26",
    "@swc-node/register": "^1.11.1",
    "@swc/core": "^1.15.41",
    "@types/better-sqlite3": "catalog:",
    "@types/express": "^5.0.6",
    "@types/node": "catalog:",
    "@types/ws": "^8.18.1",
    "typescript": "catalog:",
    "vitest": "^4.1.8"
  }
}
