# ============================================================================
# api 控制面进程（apps/api）专属配置 —— 全部可选，不填即用默认值
# ----------------------------------------------------------------------------
# 共享项（DATABASE_URL / SETTINGS_SECRET / LOG_LEVEL / HTTP_PORT / DATABASE_POOL_MAX）
# 在工作区根 .env，启动时经 --env-file-if-exists=../../.env 自动加载，勿在此重复。
# 本文件只放 api 独有项；worker 专属项见 apps/worker/.env.example。
# 用法：需要时 cp .env.example .env 并取消相应注释。
# ============================================================================

# ─── 按需启用（不填即关闭，不影响启动）──────────────────────────────────────

# 首个超管种子（仅空库首启创建，幂等）；新部署务必设，否则无账户可登录，首登强制改密
# SUPER_ADMIN_EMAIL=you@example.com
# SUPER_ADMIN_PASSWORD=change-me-at-least-8-chars

# SPA 静态产物目录，默认 ../web/dist；不存在则跳过托管（开发期走 Vite proxy）
# WEB_DIST_DIR=

# 注：分析批次 / 会话时长 / job 超时 / 僵死回收阈值等运行期参数已移入 DB
#     （首启播种、Web 设置页可改），不再走 env。
