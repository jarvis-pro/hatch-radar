# ============================================================================
# 数据面 worker 进程（apps/worker）专属配置 —— 全部可选，不填即用默认值
# ----------------------------------------------------------------------------
# 共享项（DATABASE_URL / SETTINGS_SECRET / LOG_LEVEL / HTTP_PORT / DATABASE_POOL_MAX）
# 在工作区根 .env，启动时经 --env-file-if-exists=../../.env 自动加载，勿在此重复。
# 本文件只放 worker 独有项；api 专属项见 apps/api/.env.example。
# 多机扩容：把根 .env 连同本文件带到各 worker 机（须能连同一 DATABASE_URL），
#           并按下方设 GATEWAY_URL 指向 api 主机。
# ============================================================================

# ─── worker 专属 ─────────────────────────────────────────────────────────────

# 连 api 网关的 WebSocket 地址；不设时由根 .env 的 HTTP_PORT 推导为
# ws://localhost:<HTTP_PORT>/ws/worker（仅适合与 api 同机）。跨机须显式填 api 主机地址。
# GATEWAY_URL=ws://localhost:47878/ws/worker

# 并发认领数（改后须重启 worker；连接池下限 = max(10, 此值+5)）
# WORKER_CONCURRENCY=20
